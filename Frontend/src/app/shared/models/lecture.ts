export interface Lecture {
    courseID: string,
    description: string,
    thumbnailUrl:string,
    title: string,
    uploadDate: Date,
    uploader: string,
    videoUrl: string,
    id: string
    //avalibility:boolean (public or private)
}
